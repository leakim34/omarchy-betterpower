# Before the first public release

- [x] Rename to BetterPower (`leakz.betterpower`), ADR 0005.
- [ ] Delete the stray `hero.png` and `heroc.png` (unreferenced, ~4.9 MB) and purge them from history if the repo size matters.
- [ ] README install line: use the HTTPS URL, not SSH.
- [ ] README IPC table and ADR 0002 say `setLimit`; the code exposes `setChargeLimit`. Align them.
- [ ] Set the GitHub repo description.
- [ ] Run `/release` to move Unreleased into 0.2.0 and bump `manifest.json`.
- [ ] Make the repo public.
